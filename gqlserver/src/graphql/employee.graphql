type Employee {
  id: Int
  name: String
  email: String
  dob: String
  doj: String
  created_at: String
  updated_at: String
  tasks: [Task]
  address: Address
  account: Account
  department: Department
}

type Query {
  employee(id: Int!): Employee
  employees: [Employee]
}
